pygame mac os x with homebrew

install python:
    brew install python
install deps:
    brew install sdl sdl_image sdl_mixer sdl_ttf smpeg portmidi 

workon some virtualenv:
    pip install hg+http://bitbucket.org/pygame/pygame
    (esto tarda booooocha fieja!)
done!